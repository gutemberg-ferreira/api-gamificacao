Get total user score across Nexto platform
Endpoint for Get total user score across Nexto platform
---

tags:
  - Ranking

parameters:
      - in: path
        name: "id"
        required: true
        description: "User ID"
        schema:
          type: integer

responses:
  200:
    description: Return total user score across Nexto platform

  401:
    description: 'Error: UNAUTHORIZED'
    schema:
      $ref: '#/definitions/MessageNoAuth'

security:
  - APIKeyHeader: []