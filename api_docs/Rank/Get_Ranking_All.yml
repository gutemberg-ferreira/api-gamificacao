Get ranking of users within the "NEXTO application"
Endpoint for Get ranking users within the "NEXTO application"
---

tags:
  - Ranking

responses:
  200:
    description: Return ranking of users within the "NEXTO application"

  401:
    description: 'Error: UNAUTHORIZED'
    schema:
      $ref: '#/definitions/MessageNoAuth'

security:
  - APIKeyHeader: []