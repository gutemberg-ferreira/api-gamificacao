Get an event generated by the user within the "NEXTO application" by ID
Endpoint for Get an event generated by the user within the "NEXTO application" by ID
---
tags:
  - Receive Record

parameters:
      - in: path
        name: "id"
        required: true
        description: "Event generated by the user by ID"
        schema:
          type: integer

responses:
  200:
    description: Event get successfully
    schema:
      $ref: '#/definitions/RuleEventAdd'

  401:
    description: 'Error: UNAUTHORIZED'
    schema:
      $ref: '#/definitions/MessageNoAuth'

  404:
    description: 'Error: NOT FOUND'
    schema:
      $ref: '#/definitions/MessageEventNotFound'

security:
  - APIKeyHeader: []

definitions:
  MessageEventNotFound:
    type: "object"
    properties:
      message:
        type: "string"
        example: "Event don't exist"