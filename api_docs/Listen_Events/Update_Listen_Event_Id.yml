Test Api
Endpoint for test API
---
tags:
  - Listen Events
parameters:
  - in: path
    name: "ListenEventId"
    required: true
    schema:
      type: integer
  - in: "body"
    name: "body"
    description: "User object that needs to be added to the API"
    required: true
    schema:
      $ref: "#/definitions/ListenEventAdd"
responses:
  200:
    description: Return message Hello, World!
    schema:
      $ref: '#/definitions/ListenEventAdd'

security:
  - APIKeyHeader: []