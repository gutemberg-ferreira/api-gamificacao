Update an event generated by the user within the "NEXTO application" by ID
Endpoint for Update an event generated by the user within the "NEXTO application" by ID
---
tags:
  - Receive Record
parameters:
  - in: path
    name: "ListenEventId"
    required: true
    schema:
      type: integer
  - in: "body"
    name: "body"
    description: "Object (event generated by the user) that needs to be added to the API"
    required: true
    schema:
      $ref: "#/definitions/ListenEventAdd"

responses:
  201:
    description: Event updated successfully
    schema:
      $ref: '#/definitions/ListenEventAdd'

  401:
    description: 'Error: UNAUTHORIZED'
    schema:
      $ref: '#/definitions/MessageNoAuth'

  404:
    description: 'Error: NOT FOUND'
    schema:
      $ref: '#/definitions/MessageEventNotFound'


security:
  - APIKeyHeader: []