Test Api
Endpoint for test API
---
tags:
  - Tests
responses:
  200:
    description: Return message Hello, World!
    schema:
      $ref: '#/definitions/root'

definitions:
  root:
    type: object
    properties:
      message:
        type: array
        example: "Hello, World!"