Api Sun
Endpoint for sun a Number + 5
---
tags:
  - Tests
parameters:
      - in: "body"
        name: "number"
        description: "Sum a Number + 5"
        required: true
        schema:
          $ref: "#/definitions/Number"

responses:
  200:
    description: Return Sun Number + 5
    schema:
      type: object
      example: "msg: Your result is: 'X'"

definitions:
  Number:
      type: "object"
      required:
      - "number"
      properties:
        number:
          type: "integer"
          format: "int64"
          example: 5
