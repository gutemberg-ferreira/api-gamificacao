To get all events rule
Endpoint for To get all events rule
---
tags:
  - Rule Events

responses:
  200:
    description: Return All events rule

  401:
    description: 'Error: UNAUTHORIZED'
    schema:
      $ref: '#/definitions/MessageNoAuth'

security:
  - APIKeyHeader: []